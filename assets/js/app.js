// Expanded Adhkar build 20260329-083712
const API_BASE='https://api.aladhan.com/v1';
const KAABA={lat:21.4225,lon:39.8262}; const BDC_REVERSE='https://api-bdc.net/data/reverse-geocode-client';
const qs=(s,r=document)=>r.querySelector(s), qsa=(s,r=document)=>Array.from(r.querySelectorAll(s)), LS=(k,v)=>v===undefined?localStorage.getItem(k):localStorage.setItem(k,v); let CFG=null, nextTimer=null; let loaded={adhkar:false,resources:false,learning:false,tools:false};
function setText(id,t){const e=document.getElementById(id); if(e) e.textContent=t;} function isoToDate(i){return new Date(i)} function dateToApi(d){return String(d.getDate()).padStart(2,'0')+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+d.getFullYear()} function toRad(x){return x*Math.PI/180} function toDeg(x){return x*180/Math.PI} function normalize360(x){x%=360; if(x<0)x+=360; return x}
function formatTime12h(d){try{return new Intl.DateTimeFormat('ar',{hour:'numeric',minute:'2-digit',hour12:true}).format(d)}catch(e){let h=d.getHours(),m=String(d.getMinutes()).padStart(2,'0');const suf=h>=12?'م':'ص';h=h%12||12;return `${h}:${m} ${suf}`;}}
async function fetchTimingsByCoords(date,lat,lon){const ds=dateToApi(date); const u=`${API_BASE}/timings/${ds}?latitude=${lat}&longitude=${lon}&method=${CFG.calculation.method}&school=${CFG.calculation.school}&iso8601=true`; const r=await fetch(u); const j=await r.json(); if(j.code!==200) throw new Error('API'); return j.data;}
async function fetchTimingsByCity(date,city,country){const ds=dateToApi(date); const u=`${API_BASE}/timingsByCity/${ds}?city=${encodeURIComponent(city)}&country=${encodeURIComponent(country)}&method=${CFG.calculation.method}&school=${CFG.calculation.school}&iso8601=true`; const r=await fetch(u); const j=await r.json(); if(j.code!==200) throw new Error('API'); return j.data;}
function computeDuha(s,d){return {start:new Date(isoToDate(s).getTime()+CFG.duha.startOffsetAfterSunriseMin*60000), end:new Date(isoToDate(d).getTime()-CFG.duha.endOffsetBeforeDhuhrMin*60000)}} function computeLastThird(m,f){const magh=isoToDate(m), fajr=isoToDate(f); let night=fajr-magh; if(night<=0) night+=86400000; const third=night/3; return {start:new Date(fajr.getTime()-third), end:fajr}} function bearing(lat1,lon1,lat2,lon2){const φ1=toRad(lat1),φ2=toRad(lat2),λ1=toRad(lon1),λ2=toRad(lon2); const y=Math.sin(λ2-λ1)*Math.cos(φ2),x=Math.cos(φ1)*Math.sin(φ2)-Math.sin(φ1)*Math.cos(φ2)*Math.cos(λ2-λ1); return normalize360(toDeg(Math.atan2(y,x)));}
function translatePrayer(k){return ({Fajr:'الفجر',Sunrise:'الشروق',Dhuhr:'الظهر',Asr:'العصر',Maghrib:'المغرب',Isha:'العشاء'})[k]||k;} function renderHijri(){try{const f=new Intl.DateTimeFormat('ar-SA-u-ca-islamic',{day:'numeric',month:'long',year:'numeric'}); setText('hijri',f.format(new Date()))}catch(e){setText('hijri','—')}}
function setTheme(t){document.documentElement.setAttribute('data-theme',t); LS('theme',t)} function initTheme(){const s=LS('theme')||'dark'; setTheme(s); qs('#toggleTheme').addEventListener('click',()=>setTheme(document.documentElement.getAttribute('data-theme')==='dark'?'light':'dark'));} function setScheme(s){document.documentElement.setAttribute('data-scheme',s); LS('scheme',s)} function initScheme(){const s=LS('scheme')||'brown'; setScheme(s); const sel=qs('#schemeSelect'); sel.value=s; sel.addEventListener('change',()=>setScheme(sel.value));}
function showSection(id){qsa('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.target===id)); qsa('.section').forEach(s=>s.classList.toggle('active',s.id===id));} function initNav(){qsa('.nav button').forEach(btn=>btn.addEventListener('click',async()=>{const id=btn.dataset.target; showSection(id); if(id==='adhkar'&&!loaded.adhkar){loaded.adhkar=true; await loadAdhkar();} if(id==='resources'&&!loaded.resources){loaded.resources=true; await loadResources();} if(id==='learning'&&!loaded.learning){loaded.learning=true; await loadLearning();} if(id==='tools'&&!loaded.tools){loaded.tools=true; setupTools();} window.scrollTo({top:0,behavior:'smooth'});}));} function renderFooterVersion(){setText('footerVersion',CFG.version)}
function initCityList(){const cities=[{label:'الرياض',city:'Riyadh',country:'SA'},{label:'مكة المكرمة',city:'Makkah',country:'SA'},{label:'المدينة المنورة',city:'Al Madinah al Munawwarah',country:'SA'},{label:'جدة',city:'Jeddah',country:'SA'},{label:'الدمام',city:'Dammam',country:'SA'},{label:'الطائف',city:'Taif',country:'SA'},{label:'أبها',city:'Abha',country:'SA'},{label:'تبوك',city:'Tabuk',country:'SA'}]; const sel=qs('#citySelect'); cities.forEach(c=>{const o=document.createElement('option'); o.value=JSON.stringify(c); o.textContent=c.label; sel.appendChild(o);}); const saved=LS('cityFallback'); if(saved) sel.value=saved; sel.addEventListener('change',()=>{LS('cityFallback',sel.value); updateCityKPIFromSelect();}); updateCityKPIFromSelect();}
function getCityFallback(){const v=LS('cityFallback'); if(v) try{return JSON.parse(v)}catch(e){} return CFG.defaultCity;} function updateCityKPI(t){setText('cityDisplay',t||'—')} function updateCityKPIFromSelect(){const sel=qs('#citySelect'); if(sel.value) try{const o=JSON.parse(sel.value); updateCityKPI(o.label||o.city)}catch(e){} else {const c=getCityFallback(); updateCityKPI(c.label||c.city||'—')}} function setLocationMode(a){const wrap=qs('#cityControls'), chip=qs('#locChip'), btn=qs('#useLocation'); if(a){wrap.style.display='none'; btn.style.display='none'; chip.style.display='inline-flex'; chip.textContent='الموقع مفعل ✓';} else {wrap.style.display='flex'; btn.style.display='inline-flex'; chip.style.display='none';}}
async function reverseGeocodeCity(lat,lon){const key=`rg:${lat.toFixed(3)},${lon.toFixed(3)}`; const cached=LS(key); if(cached) try{return JSON.parse(cached)}catch(e){} const u=`${BDC_REVERSE}?latitude=${encodeURIComponent(lat)}&longitude=${encodeURIComponent(lon)}&localityLanguage=ar`; const r=await fetch(u); const j=await r.json(); const out={city:j.city||j.locality||j.principalSubdivision||null}; LS(key,JSON.stringify(out)); return out;}
function renderNextPrayer(T,fajrTomorrowISO){const order=['Fajr','Dhuhr','Asr','Maghrib','Isha']; const now=new Date(); let name=null,time=null; for(const k of order){const d=isoToDate(T[k]); if(d>now){name=k; time=d; break;}} if(!name){name='Fajr'; time=isoToDate(fajrTomorrowISO);} setText('nextPrayerName',translatePrayer(name)); setText('nextPrayerTime',formatTime12h(time)); if(nextTimer) clearInterval(nextTimer); nextTimer=setInterval(()=>{const diff=time-new Date(); if(diff<=0){setText('nextCountdown','حان الوقت'); clearInterval(nextTimer); return;} const h=Math.floor(diff/3600000),m=Math.floor((diff%3600000)/60000),s=Math.floor((diff%60000)/1000); setText('nextCountdown',`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`);},1000);}
function setQiblaFromCoords(lat,lon){const b=bearing(lat,lon,KAABA.lat,KAABA.lon); setText('qiblaDeg',`${b.toFixed(1)}°`); LS('qiblaBearing',String(b));} function loadStoredQibla(){const v=LS('qiblaBearing'); if(v) setText('qiblaDeg',`${parseFloat(v).toFixed(1)}°`)}
async function loadPrayerTimes(forceCity=false){setText('ptStatus',''); setText('ptMeta',''); const today=new Date(), tomorrow=new Date(Date.now()+86400000), c=getCityFallback(); try{let td,td2; if(!forceCity&&'geolocation' in navigator){setLocationMode(true); updateCityKPI('جاري تحديد المدينة…'); const pos=await new Promise((res,rej)=>navigator.geolocation.getCurrentPosition(res,rej,{enableHighAccuracy:true,timeout:12000,maximumAge:600000})); const lat=pos.coords.latitude, lon=pos.coords.longitude; setText('ptMeta',`دقة الموقع: ±${Math.round(pos.coords.accuracy||0)}م`); const rg=reverseGeocodeCity(lat,lon).catch(()=>null); td=await fetchTimingsByCoords(today,lat,lon); td2=await fetchTimingsByCoords(tomorrow,lat,lon); const city=await rg; updateCityKPI(city&&city.city?city.city:'موقعي'); setQiblaFromCoords(lat,lon);} else {setLocationMode(false); updateCityKPI(c.label||c.city); td=await fetchTimingsByCity(today,c.city,c.country); td2=await fetchTimingsByCity(tomorrow,c.city,c.country);} const T=td.timings, TT=td2.timings; ['Fajr','Sunrise','Dhuhr','Asr','Maghrib','Isha'].forEach(k=>setText('t_'+k.toLowerCase(),formatTime12h(isoToDate(T[k])))); const duha=computeDuha(T.Sunrise,T.Dhuhr); setText('t_duha',`${formatTime12h(duha.start)} – ${formatTime12h(duha.end)}`); const last=computeLastThird(T.Maghrib,TT.Fajr); setText('t_lastthird',`${formatTime12h(last.start)} – ${formatTime12h(last.end)}`); renderNextPrayer(T,TT.Fajr);}catch(e){console.warn(e); setLocationMode(false); setText('ptStatus','تعذّر تحديد الموقع/جلب المواقيت. جرّب اختيار مدينة.');}}
function setupCompass(){const needle=qs('#needle'), acc=qs('#compassAccuracy'); let ema=null; function delta(a,b){return (b-a+540)%360-180} function render(q,h){needle.style.transform=`translate(-50%,-100%) rotate(${normalize360(q-h)}deg)`;} function onOri(ev){let heading=null; if(typeof ev.webkitCompassHeading==='number'&&ev.webkitCompassHeading>=0){heading=ev.webkitCompassHeading; if(typeof ev.webkitCompassAccuracy==='number') acc.textContent=`دقة البوصلة: ±${Math.round(ev.webkitCompassAccuracy)}°`;} else if(typeof ev.alpha==='number'){heading=360-ev.alpha; acc.textContent='دقة البوصلة: تقريبية';} if(heading==null) return; const q=parseFloat(LS('qiblaBearing')||'0')||0; if(ema==null) ema=heading; ema=normalize360(ema+delta(ema,heading)*0.18); render(q,ema);} qs('#enableCompass').addEventListener('click',async()=>{try{if(window.DeviceOrientationEvent&&typeof DeviceOrientationEvent.requestPermission==='function'){const p=await DeviceOrientationEvent.requestPermission(); if(p!=='granted') return;} window.addEventListener('deviceorientation',onOri,true); setText('compassStatus','تم تفعيل البوصلة');}catch(e){setText('compassStatus','تعذّر تفعيل البوصلة');}});}
function dayKey(){return new Date().toDateString()} function formatRepeat(rem){return rem===0?'تم':`التكرار: ${rem}`} function renderDhikrList(container,list,keyPrefix){container.innerHTML=''; (list||[]).forEach((it,i)=>{const max=typeof it.repeat==='number'?it.repeat:(it.repeat||1); const numeric=typeof max==='number'; const k=`dhikr:${keyPrefix}:${i}:${dayKey()}`; let rem=LS(k); rem=rem==null?(numeric?max:1):parseInt(rem,10); const pct=numeric?Math.round(((max-rem)/max)*100):(rem?0:100); const card=document.createElement('div'); card.className='dhikr-card'; card.innerHTML=`<p class="dhikr-text">${it.text}</p><div class="small">المصدر: ${it.source||'—'}</div><div class="progress"><div style="width:${pct}%"></div></div><div class="actions"><div class="left"><button class="btn secondary copy">نسخ</button><a class="btn secondary" href="${it.ref||'#'}" target="_blank" rel="noopener">مرجع</a></div><button class="btn do">${numeric?formatRepeat(rem):('التكرار: '+max)}</button></div>`; card.querySelector('.copy').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(it.text)}catch(e){}}); const btn=card.querySelector('.do'), bar=card.querySelector('.progress>div'); btn.addEventListener('click',()=>{if(!numeric) return; if(rem<=0) return; rem-=1; LS(k,String(rem)); const p=Math.round(((max-rem)/max)*100); bar.style.width=p+'%'; btn.textContent=formatRepeat(rem); if(navigator.vibrate) navigator.vibrate(rem===0?[120,50,120]:40);}); container.appendChild(card);})}
async function loadAdhkar(){const data=await (await fetch('./data/adhkar.json')).json(); const tabs=[{key:'morning',label:'الصباح'},{key:'evening',label:'المساء'},{key:'sleep',label:'النوم'},{key:'wakeup',label:'الاستيقاظ'},{key:'afterPrayer',label:'بعد الصلاة'},{key:'home',label:'المنزل'},{key:'mosque',label:'المسجد'},{key:'daily',label:'متفرقة'}]; const pills=qs('#adhkarPills'), container=qs('#adhkarContainer'); function activate(key){qsa('#adhkarPills button').forEach(b=>b.classList.toggle('active',b.dataset.key===key)); renderDhikrList(container,data[key],key);} pills.innerHTML=''; tabs.forEach(t=>{const b=document.createElement('button'); b.textContent=t.label; b.dataset.key=t.key; b.addEventListener('click',()=>activate(t.key)); pills.appendChild(b);}); activate('morning');}
async function loadResources(){const data=await (await fetch('./data/resources.json')).json(); const host=qs('#usefulLinks'); host.innerHTML=''; (data.useful||[]).forEach(g=>{const sec=document.createElement('div'); sec.className='dhikr-card'; sec.innerHTML=`<h3 style="margin:0 0 8px">${g.group}</h3>`; const ul=document.createElement('ul'); (g.items||[]).forEach(it=>{const li=document.createElement('li'); li.innerHTML=`<a href="${it.url}" target="_blank" rel="noopener">${it.title}</a> <span class="small">— ${it.desc||''}</span>`; ul.appendChild(li);}); sec.appendChild(ul); host.appendChild(sec);});}
async function loadLearning(){const data=await (await fetch('./data/learning.json')).json(); const plan=qs('#learnPlan'); plan.innerHTML=''; (data.plan||[]).forEach(it=>{const d=document.createElement('div'); d.className='dhikr-card'; d.innerHTML=`<b>${it.title}</b><div class="small">${it.tip}</div>`; plan.appendChild(d);}); const col=qs('#learnCollections'); col.innerHTML=''; (data.collections||[]).forEach(it=>{const li=document.createElement('li'); li.innerHTML=`<a href="${it.url}" target="_blank" rel="noopener">${it.title}</a>`; col.appendChild(li);}); const rem=qs('#learnReminders'); rem.innerHTML=''; (data.reminders||[]).forEach(t=>{const li=document.createElement('li'); li.textContent=t; rem.appendChild(li);});}
function setupTools(){const nisab=qs('#nisab'), amount=qs('#amount'), out=qs('#zakatOut'); function calc(){const n=parseFloat(nisab.value||'0'), a=parseFloat(amount.value||'0'); if(!n||!a){out.textContent='—'; return;} if(a<n){out.textContent='لا زكاة (أقل من النصاب)'; return;} out.textContent=`الزكاة: ${(a*0.025).toFixed(2)}`;} nisab.addEventListener('input',calc); amount.addEventListener('input',calc);}
function showUpdateBar(reg){const bar=qs('#updateBar'); bar.style.display='flex'; qs('#updateNow').onclick=()=>{if(reg.waiting) reg.waiting.postMessage({type:'SKIP_WAITING'});}; qs('#updateLater').onclick=()=>{bar.style.display='none';};}
async function registerSW(){if(!('serviceWorker' in navigator)) return; const reg=await navigator.serviceWorker.register('./service-worker.js',{scope:'./'}); try{await reg.update();}catch(e){} navigator.serviceWorker.addEventListener('controllerchange',()=>window.location.reload()); if(reg.waiting) showUpdateBar(reg); reg.addEventListener('updatefound',()=>{const sw=reg.installing; if(!sw) return; sw.addEventListener('statechange',()=>{if(sw.state==='installed'&&navigator.serviceWorker.controller) showUpdateBar(reg);});});}
async function init(){CFG=await (await fetch('./assets/js/config.json')).json(); initTheme(); initScheme(); initNav(); initCityList(); renderHijri(); renderFooterVersion(); loadStoredQibla(); setupCompass(); qs('#useLocation').addEventListener('click',()=>loadPrayerTimes(false)); qs('#useCity').addEventListener('click',()=>loadPrayerTimes(true)); await loadPrayerTimes(false); await registerSW();}
window.addEventListener('load',init);
